# Lucky7 웹 — 이 저장소에서 일할 때

`index.html` 한 장짜리 앱이다. 7만 줄이 넘고, 배포는 곧 전 직원 화면이다.

## 배포 전에 반드시 (2026-09-12 하루에 세 번 당하고 만든 규칙)

1. **정적 검사** — 통과해야 커밋한다
   ```
   perl ~/.claude/scheduled-tasks/lucky7-web-quality/check.pl index.html
   ```
   CSS 주석이 먼저 닫힌 자리 · 한글 깨짐 · BUILD 줄 · script 여닫기를 본다.

2. **구문 검사** — `serve8760.ps1` 을 켜고 브라우저에서 `<script>` 덩어리마다
   `new Function()`. **index 4 는 HTML 이라 늘 실패한다 — 그 하나만 실패해야 한다.**

3. **화면 바꾼 패치는 띄워서 잰다.** 색·폭·위치는 눈으로 보면 놓친다.
   실제 스타일시트로 요소를 띄우고 `getComputedStyle` 로 기대값과 대조한다.
   v1506 은 "보라로 바꿨다"고 보고했지만 실제로는 절반만 먹었고,
   v1513 은 단추 규칙이 통째로 버려졌는데 화면에는 단추가 보였다.

## CSS 함정 둘 — 둘 다 조용히 실패한다

· **주석 안에 닫는 기호를 글로도 쓰지 마라.** 거기서 주석이 끝나고, 뒤에 이어 쓴
  설명이 CSS 로 읽히면서 **그 아래 규칙 한 덩어리가 통째로 버려진다.**
  화면에는 요소가 보이고 색만 맞아서 전혀 다른 문제로 보인다.

· **힘이 같으면 나중에 깔린 쪽이 이긴다.** 이 파일의 CSS 는 두 곳에 있다 —
  머리의 `<style>` 과 **스크립트가 나중에 깔아 넣는 문자열 배열**.
  `#sr-app .sr-clcard-li` 로는 `#sr-app .sr-clcard` 를 못 이긴다(둘 다 아이디1+클래스1).
  클래스를 하나 더 얹어라: `#sr-app .sr-clcard.sr-clcard-li`.

## 패치하는 법

`perl -pi` 를 index.html 에 직접 쓰지 마라 — 이중 인코딩이 난다.
**펄 스크립트 파일**을 써서 `q~...~` 리터럴로 치환하고, 바꾼 수를 세서 1이 아니면 죽인다.
`~` 문자를 내용에 넣지 마라(리터럴이 거기서 끝난다). 한글 패턴은 `-pe` 로는 조용히 실패한다.

## 배포

```
7번 줄 <!-- BUILD: vNNNN | 한 줄 설명 --> 을 올린다
git add index.html && git commit -F <메시지파일> && git push origin main
curl 로 https://teamlucky77777-cyber.github.io/Lucky-7-Web/ 7번 줄을 확인한다
```
푸시한 뒤 답하기 전에 **로드맵 아티팩트를 갱신한다**(사장님 상시 지시).
